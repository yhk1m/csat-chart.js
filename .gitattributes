# 저장소 안에서는 LF 로 통일한다. autocrlf 설정이 다른 기여자와 리눅스 CI 가
# 같은 바이트를 보게 하기 위해서다.
* text=auto eol=lf

# 골든 이미지는 반드시 이진 파일로 다룬다. 뒤에 오는 규칙이 이기므로
# 반드시 위의 포괄 규칙 «아래» 에 둔다.
# git 의 내용 추론(첫 8000바이트에 NUL 이 있는가)에 기대면, 단순한 그림이
# 우연히 그 조건을 벗어났을 때 CRLF 변환으로 조용히 망가진다.
*.png binary
